{
  "require": "ts-node/register,source-map-support/register",
  "watch-extensions": "ts",
  "watch-files": ["test/**/*.ts", "src/**/*.ts"],
  "recursive": true,
  "reporter": "spec",
  "timeout": 5000
}